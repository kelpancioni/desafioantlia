import { BusinessRepository } from './business-repository';

describe('BusinessRepository', () => {
  it('should create an instance', () => {
    expect(new BusinessRepository()).toBeTruthy();
  });
});
